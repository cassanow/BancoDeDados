@{
    ViewData["Title"] = "Home Page";
}

<div class="d-flex justify-content-center align-items-center min-vh-100">
    <div class="container text-center">
        <h1 class="display-4 mb-5">Sistema de Cadastro de Usuários</h1> 
        <p><a href="@Url.Action("Criar", "Pessoa")" class="btn btn-primary w-50">Cadastrar Nova Pessoa</a></p>
        <p><a href="@Url.Action("Editar", "Pessoa")" class="btn btn-primary w-50">Editar</a></p>
        <p><a href="@Url.Action("Remover", "Pessoa")" class="btn btn-primary w-50">Remover</a></p>
        <p><a href="@Url.Action("PessoasCadastradas", "Pessoa")" class="btn btn-primary w-50">Ver Pessoas Cadastradas</a></p>
    </div>
</div>
